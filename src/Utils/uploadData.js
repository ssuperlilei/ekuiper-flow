const uploadNodes = (nodes, edges) => {
  console.log(nodes, edges);
  const data = {
    id: 'rule1',
    name: '规则ui',
    graph: {
      nodes: {},
      topo: {
        source: [],
        edges: [],
      },
    },
  };
};

export default uploadNodes;
